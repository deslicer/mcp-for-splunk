# Agent Tracing Guide (removed)

## Status

OpenAI Agents SDK tracing for `workflow_runner` was **removed** with the built-in agent execution path.

For current observability options, see optional OpenTelemetry support in the main project docs (`OTEL_EXPORTER_OTLP_ENDPOINT`) and [Workflows Guide](README.md).
